spring:
  application:
    name: Projeto-e-commerce
  datasource:
    url: jdbc:mysql://localhost:3306/projeto_e_commerce?useTimezone=true&serverTimezone=UTC
    username: root
    password: Neu.1970
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
